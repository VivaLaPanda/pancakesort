package permutation

import (
	"testing"
)

func TestMakeNode(t *testing.T) {
	actual := true
	expected := true

	if actual != expected {
		t.Errorf("Error occured while testing Childen: '%s' != '%s'", expected, actual)
	}
}

func TestChildren(t *testing.T) {
	actual := true
	expected := true

	if actual != expected {
		t.Errorf("Error occured while testing Childen: '%s' != '%s'", expected, actual)
	}
}

func TestIsGoal(t *testing.T) {
	actual := true
	expected := true

	if actual != expected {
		t.Errorf("Error occured while testing IsGoal: '%s' != '%s'", expected, actual)
	}
}

func TestDump(t *testing.T) {
	actual := true
	expected := true

	if actual != expected {
		t.Errorf("Error occured while testing Dump: '%s' != '%s'", expected, actual)
	}
}
